# Rust build output
target/

# TLC scratch + counterexample litter (SEED s§2 notes)
states/
*_TTrace_*.tla
*_TTrace_*.bin

# TLA+ tooling jars, fetched by `just tla-install` (scripts/tla.mjs)
specs/.tools/

# P tool, fetched by `just p-install` (scripts/p.mjs, #131); build output
# from `p compile`/`p check`, generated anywhere under p/
p/.tools/
PGenerated/
PCheckerOutput/

# Bun
node_modules/

# local working dirs (never committed)
.reviews/
.sections/

# local working copy; canonical copy is docs/seed.md
/SEED.md
.claude/

# cargo-mutants scratch output (ledger row mutation-floor, issue #49)
mutants.out/
